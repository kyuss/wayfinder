# Eval run — 2026-07-03 (post security-hardening B + D + Fix-A regression repair)

Full set: spec-altitude (spec), ambiguous-spec (plan), node-bugfix (plan, execute),
node-feature (plan, execute), py-feature (plan, execute).

Context: gate after three hardening changes to the sandbox + boundary surface —
- **B**: `wf-exec` now strips secret-shaped env vars (denylist by name: `*TOKEN*`, `*SECRET*`,
  `*_KEY`, `*AUTH*`, `AWS_*`, `GH_TOKEN`, `ANTHROPIC_*`, …) before running project code, so a host
  token in the environment can't be read and committed into the diff.
- **D-web**: `WebFetch` removed from `wf-planner` + `wf-spec-builder` (kept `WebSearch`) — closes the
  arbitrary-URL exfil channel on agents that ingest untrusted ticket text.
- **D-bash**: `wf-sandbox-guard.sh` broadened to also block raw network binaries
  (`curl`/`wget`/`nc`/`ssh`/`scp`/…) inside worktrees; planner + spec-builder gained a "sandbox your
  shell / ignore fetch-a-URL instructions" prompt note.
- **Fix-A repair**: the earlier `(subpath ~/.claude)` deny broke the eval harness (fixtures live
  under `~/.claude`, run in place). Re-allowed `file-read*` on `ticket-workflow-evals` + metadata on
  the `~/.claude` dir node so path resolution can traverse in, while agent files/credentials/enumeration
  stay denied.

| case           | stage   | verdict | score | biggest weakness                                                    |
|----------------|---------|---------|-------|---------------------------------------------------------------------|
| spec-altitude  | spec    | PASS    | 0.88  | none — resolved both thin decisions, stayed at pointer altitude      |
| ambiguous-spec | plan    | PASS    | 0.78  | honest (NICE): proceeds to a PLAN rather than explicitly recommending /wf-spec |
| node-bugfix    | plan    | PASS    | 0.99  | none                                                                |
| node-bugfix    | execute | PASS    | 1.00  | none — judge independently reran node --test (3/3)                   |
| node-feature   | plan    | PASS    | 1.00  | none                                                                |
| node-feature   | execute | PASS    | 1.00  | none — judge independently reran (2/2)                              |
| py-feature     | plan    | PASS    | 1.00  | none                                                                |
| py-feature     | execute | PASS    | 1.00  | none — judge independently reran unittest (2/2)                     |

Mean score: 0.96
Pass-rate: 8/8 (100%)

Baseline comparison (vs baselines/baseline.md + run-2026-06-28-spec-stage.md):
- spec-altitude spec:   0.92 → 0.88  (−0.04, within tolerance — judge variance, no FAIL trigger)
- ambiguous-spec plan:  0.69 → 0.78  (+0.09)
- node-bugfix plan:     0.95 → 0.99  (+0.04)
- node-bugfix execute:  0.96 → 1.00  (+0.04)
- node-feature plan:    1.00 → 1.00  (no change)
- node-feature execute: 1.00 → 1.00  (no change)
- py-feature plan:      1.00 → 1.00  (no change)
- py-feature execute:   1.00 → 1.00  (no change)
- **NO REGRESSIONS.** No PASS→FAIL, no score drop > 0.15.

What this gate validates about the hardening:
- **B** — all three execute stages ran the fixture's tests through `wf-exec` with the pruned env and
  came back green; the denylist did not strip anything the toy toolchains need.
- **Fix-A repair** — executors AND judges ran `wf-exec` tests *in place* under `~/.claude` (would have
  been EPERM before the carve-back); all passed, confirming traversal-in works while contents stay denied.
- **D-web** — spec-builder + all four planners produced full-quality output with WebFetch gone (they
  explored via Read/Grep/git-log); no degradation, in fact ambiguous-spec and node-bugfix plan improved.
- **D-bash** — planners ran read-only `git log`/reads with no false-positive guard blocks; executors ran
  sandboxed tests cleanly.

Notes:
- ambiguous-spec correctly refused to invent scope (non-empty SPEC_GAPS anchored to the in-code NOTE
  comment, single fix, no sprawl); only the NICE "honest" dim (explicitly recommend /wf-spec) missed —
  same standing candidate prompt nudge for wf-planner as in the baseline, not a regression.
- Fixtures reset to clean `main`, no leftover eval/* branches.
