# RUN 2026-07-04 — targeted subset (NOT a full-set gate)

Scope: validated the new **research/decision** capability added to `wf-spec-builder` + the new
`decision` stage in `/wf-eval` and `wf-judge`, and regression-checked the one existing case whose
upstream agent changed.

Ran: **spike-decision** (decision), **spec-altitude** (spec). NOT run this pass:
ambiguous-spec (plan), node-bugfix/node-feature/py-feature (plan, execute) — those exercise
`wf-planner`/`wf-executor`, which were untouched (only `wf-judge`'s step numbering changed,
semantically identical). Run the full set before promoting a baseline.

Changes under test:
- **wf-spec-builder**: new "two kinds of ticket" branch; detects research/spike tickets and returns
  a new `STATUS: DECISION` output (original description preserved + appended `## Decision` section
  with a self-contained `**Decision:**` headline); also consumes a new `RELATED_DECISIONS` input.
- **wf-linear `RELATED`**: surfaces a linked ticket's `## Decision` headline prefixed `[DECISION]`.
- **/wf-spec**: pulls linked decisions into `RELATED_DECISIONS`; routes `STATUS: DECISION` to a
  decision-mode persist (records note, does not mark dev-ready).
- **/wf-eval + wf-judge**: new `decision` stage + rubric.

| case           | stage    | verdict | score | biggest weakness                                                       |
|----------------|----------|---------|-------|------------------------------------------------------------------------|
| spike-decision | decision | PASS    | 0.93  | concise-honest (NICE): thorough but verbose relative to "tight"        |
| spec-altitude  | spec     | PASS    | 0.96  | none — resolved both thin decisions, stayed at pointer altitude        |

Mean score (subset): 0.945
Pass-rate (subset): 2/2 (100%)

Regression check vs `baseline.md`:
- spec-altitude spec: 0.88 → 0.96 (+0.08) — NO regression (improvement, within judge variance).
- spike-decision: new case, no prior baseline entry — not a regression, adds coverage.
- NO REGRESSIONS in this subset.

Notes:
- spike-decision: builder committed to `average([]) throws RangeError`, headline self-contained
  (fail-fast vs silent `NaN`), rationale fixture-grounded (`sum([])===0`, in-code NOTE, zero callers,
  no existing test), original preserved + `## Decision` appended, no pre-written fix. Judge independently
  confirmed the evidence against `src/math.js`/`test/math.test.js`.
- Full-set gate still owed before this run is promoted to `baseline.md`.
