# Eval run — 2026-07-08

Scope: single case (`spec-altitude`, stage `spec`) — smoke check for the wf-spec-builder prompt change
(environment-agnostic output rule: repo-relative paths, plain command names in anything published to Linear).
Other stages/agents were not prompt-changed in a judged path (wf-verifier gained an integration mode,
wf-runner a GATES output line, wf-github PUSH/COMMENT_PR intents, /wf-run the integration stage +
unified inner loop + tighter PR template — none of these are covered by existing stage evals).

| case          | stage | verdict | score | biggest weakness |
|---------------|-------|---------|-------|------------------|
| spec-altitude | spec  | PASS    | 0.93  | implementation notes dense, hug the "how" boundary (within rubric allowance) |

Mean score: 0.93 · Pass rate: 1/1

## Regression gate vs baseline.md
- spec-altitude spec: 0.88 → 0.93 (+0.05, no regression)

## Notes
- First run of the new rule (rule only in the Output preamble) still produced absolute local paths
  (`/Users/…/fixtures/node-toy/src/math.js`) — scored PASS 0.88, identical to baseline. The rule was then
  strengthened with an explicit "Repo-relative paths only" bullet in "Rules for the spec"; the re-run above
  produced fully repo-relative paths and plain commands. The recorded verdict is the re-run (final prompt).
- The new /wf-run integration stage and wf-verifier MODE: INTEGRATION have no eval case yet — worth adding
  a case + fixture integration suite so the gate covers them.
