# RUN 2026-07-04 (14:26) — targeted subset (NOT a full-set gate)

Scope: gate after a batch of agent/command improvements, plus first exercise of the new
**review** stage (`wf-reviewer` coverage — previously the only pipeline agent with zero
regression coverage).

Changes under test:
- **wf-planner**: CONTEXT_PACK `commands` now asks for the single-test form; new honesty nudge —
  a visibly un-spec'd ticket must put `ticket looks un-spec'd — run /wf-spec first` as the first
  `SPEC_GAPS` entry (the standing candidate from the 2026-07-03 baseline notes).
- **wf-executor**: "verify narrowly while you work" — single-test form per step; full suite
  belongs to the verifier.
- **wf-reviewer**: now receives `HANDOFF` and checks design-handoff fidelity + provenance
  (verbatim-copied handoff files/markup = BLOCKING); wording updated for concurrent verify.
- **wf-runner + /wf-run (mirrored)**: review + verify now run **concurrently**; fix rounds are
  **combined** (reviewer BLOCKING + verifier FAILURES in one executor pass, ≤2 rounds, both
  re-run after a fix).
- **/wf-eval + wf-judge + new case `node-review-bug`**: new `review` stage — planted-flaw diff
  (wrong guard, hollow test, unrelated churn) applied to node-toy; reviewer's findings judged
  against `RUBRIC:review`.
- **/wf-spec**: step-3 context fetches (PR_HISTORY + RELATED) spawned concurrently.

| case            | stage   | verdict | score | biggest weakness                                                        |
|-----------------|---------|---------|-------|--------------------------------------------------------------------------|
| ambiguous-spec  | plan    | PASS    | 0.75  | honest (NICE): still no explicit /wf-spec recommendation — see caveat     |
| node-bugfix     | plan    | PASS    | 0.98  | none — nit: NOTE-comment instruction slightly non-committal               |
| node-bugfix     | execute | PASS    | 0.99  | none — judge independently reran node --test (3/3)                        |
| node-review-bug | review  | PASS    | 1.00  | none — all 3 planted flaws caught, correct severity routing, zero noise   |

Mean score (subset): 0.93
Pass-rate (subset): 4/4 (100%)

Regression check vs `baseline.md`:
- ambiguous-spec plan: 0.78 → 0.75 (−0.03, within 0.15 tolerance, no PASS→FAIL) — NO regression.
- node-bugfix plan:    0.99 → 0.98 (−0.01) — NO regression.
- node-bugfix execute: 1.00 → 0.99 (−0.01) — NO regression.
- node-review-bug review: new case/stage, no prior baseline entry — adds coverage.
- NO REGRESSIONS in this subset.

**Caveat — cached agent definitions.** This run was executed in the same session that edited the
agent files, and subagent definitions appear to be **cached at session start**: the ambiguous-spec
planner did not emit the new verbatim `run /wf-spec first` SPEC_GAPS entry, while the judge
(reading `wf-planner.md` off disk) quoted the new instruction. So this subset gates the *old*
prompts + the new harness stage; the prompt-level changes (planner honesty nudge, executor
narrow-test, reviewer handoff checks) need a **fresh-session `/wf-eval`** to be truly gated.
Expect the ambiguous-spec `honest` NICE dim to flip to PASS there.

Not run this pass: spec-altitude (spec), spike-decision (decision), node-feature (plan/execute),
py-feature (plan/execute) — their upstream agents changed only in the narrow-test/honesty prompts
covered by the cases above. Run the full set (fresh session) before promoting a baseline.
