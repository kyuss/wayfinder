# RUN — 2026-07-03 21:54:00

Full stage-eval set, offline (fixtures only; no Linear/GitHub, no PR). All project code ran through
`wf-exec` (sandbox). Fixtures reset to clean `main` before and after; throwaway `eval/<id>` branches
created per execute stage and deleted afterward. Both fixtures verified clean on `main` at end.

Full set: spec-altitude (spec), ambiguous-spec (plan), node-bugfix (plan, execute),
node-feature (plan, execute), py-feature (plan, execute).

Context: routine regression gate, no agent/model/flow change since the promoted baseline
(itself a promoted copy of run-2026-07-03.md). This run diffs against `baseline.md`.

| case           | stage   | verdict | score | biggest weakness                                                                 |
|----------------|---------|---------|-------|----------------------------------------------------------------------------------|
| spec-altitude  | spec    | PASS    | 0.95  | none — resolved both thin decisions (empty→NaN convention, even→mean of two middles) as facts; stayed at pointer altitude |
| ambiguous-spec | plan    | PASS    | 0.81  | honest (NICE): implies ticket lacks a real spec but never explicitly says "not implementation-ready / recommend /wf-spec"; proceeds to PLAN |
| node-bugfix    | plan    | PASS    | 0.94  | leaves stale NOTE-comment removal as optional ("may be removed") rather than a definite instruction — minor executor ambiguity, no functional impact |
| node-bugfix    | execute | PASS    | 1.00  | none — judge independently reran `node --test` (3/3); surgical, stale NOTE removed |
| node-feature   | plan    | PASS    | 1.00  | none                                                                             |
| node-feature   | execute | PASS    | 1.00  | none — judge independently reran `node --test` (3/3); only the two files touched  |
| py-feature     | plan    | PASS    | 0.98  | nit: lists "run tests" as a plan step rather than pure verification — stylistic   |
| py-feature     | execute | PASS    | 1.00  | none — judge independently reran `python3 -m unittest` (2/2); negative case covered |

Mean score: 0.96
Pass-rate: 8/8 (100%)

Diff vs baseline.md (regression gate — flag PASS→FAIL or score drop > 0.15):
- spec-altitude spec:   0.88 → 0.95  (+0.07)
- ambiguous-spec plan:  0.78 → 0.81  (+0.03)
- node-bugfix plan:     0.99 → 0.94  (−0.05, within tolerance — judge variance, no FAIL trigger)
- node-bugfix execute:  1.00 → 1.00  (no change)
- node-feature plan:    1.00 → 1.00  (no change)
- node-feature execute: 1.00 → 1.00  (no change)
- py-feature plan:      1.00 → 0.98  (−0.02, within tolerance)
- py-feature execute:   1.00 → 1.00  (no change)

**NO REGRESSIONS.** No PASS→FAIL transitions; no score drop exceeding the 0.15 threshold.

Notes:
- ambiguous-spec again correctly refused to invent scope: non-empty SPEC_GAPS anchored to the in-code
  `NOTE` on `average([])`, single conservative fix (guard empty case), explicitly declined new utilities
  (min/max/median). Only the NICE "honest" dim (explicitly recommend `/wf-spec`) missed — a standing
  candidate prompt nudge for wf-planner, not a regression.
- Two small score dips (node-bugfix plan −0.05, py-feature plan −0.02) are judge variance on NICE/nit
  dimensions, not behavioral regressions — both plans were correct, minimal, and fixture-accurate.
